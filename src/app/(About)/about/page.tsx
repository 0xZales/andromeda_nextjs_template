import Banner from '@/components/ui/Banner'
import { FC } from 'react';
interface pageProps {
};

const page= async ({  }) => {

  return (
    <main>
      <Banner>
        
      </Banner>
    </main>
  );
};

export default page;